export interface MovieDataProps {
  title: string;
  description: string;
  videoUrl: string;
  thumbnailUrl: string;
  genre: string;
  duration: string;
}

export interface UserCreateProps {
  name: string;
  email: string;
  password: string;
}
